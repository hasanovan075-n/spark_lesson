
services:
  minio:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    container_name: minio
    command: server /data --console-address ":9001"
    env_file:
      - .env
    environment:
      MINIO_SERVER_URL: ${MINIO_SERVER_URL}
      MINIO_BROWSER_REDIRECT_URL: ${MINIO_BROWSER_REDIRECT_URL}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    networks:
      - lake
    restart: unless-stopped
    healthcheck:

      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  spark-master:
    image: apache/spark:3.5.2
    container_name: spark-master
    environment:
      - SPARK_NO_DAEMONIZE=true
    command: ["/opt/spark/sbin/start-master.sh"]
    ports:
      - "7077:7077"
      - "8080:8080"
    volumes:
      - ./spark-defaults.conf:/opt/spark/conf/spark-defaults.conf:ro
      - spark_events:/opt/spark-events
    networks:
      - lake
    restart: unless-stopped
    healthcheck:

      test: ["CMD-SHELL", "bash -c 'cat < /dev/null > /dev/tcp/127.0.0.1/8080'"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  spark-worker-1:
    image: apache/spark:3.5.2
    container_name: spark-worker-1
    depends_on:
      spark-master:
        condition: service_healthy
    environment:
      - SPARK_NO_DAEMONIZE=true
    command: ["/opt/spark/sbin/start-worker.sh", "spark://spark-master:7077"]
    ports:
      - "8081:8081"
    volumes:
      - ./spark-defaults.conf:/opt/spark/conf/spark-defaults.conf:ro
      - spark_events:/opt/spark-events
    networks:
      - lake
    restart: unless-stopped

  spark-worker-2:
    image: apache/spark:3.5.2
    container_name: spark-worker-2
    depends_on:
      spark-master:
        condition: service_healthy
    environment:
      - SPARK_NO_DAEMONIZE=true
    command: ["/opt/spark/sbin/start-worker.sh", "spark://spark-master:7077"]
    ports:
      - "8082:8082"
    volumes:
      - ./spark-defaults.conf:/opt/spark/conf/spark-defaults.conf:ro
      - spark_events:/opt/spark-events
    networks:
      - lake
    restart: unless-stopped

  spark-history:
    image: apache/spark:3.5.2
    container_name: spark-history
    depends_on:
      spark-master:
        condition: service_healthy
    environment:
      - SPARK_NO_DAEMONIZE=true
    user: "0:0"
    command: >
      bash -lc "mkdir -p /spark-events &&
                chmod -R 777 /spark-events &&
                /opt/spark/sbin/start-history-server.sh"
    ports:
      - "18080:18080"
    volumes:
      - ./spark-defaults.conf:/opt/spark/conf/spark-defaults.conf:ro
      - spark_events:/spark-events
    networks:
      - lake
    restart: unless-stopped

  jupyter:

    image: quay.io/jupyter/pyspark-notebook:spark-3.5.2
    container_name: jupyter
    depends_on:
      spark-master:
        condition: service_healthy
      minio:
        condition: service_healthy
    ports:
      - "8888:8888"
      - "4040:4040"
    volumes:
      - ./notebooks:/home/jovyan/work
      - spark_events:/spark-events
    environment:
      - SPARK_MASTER=spark://spark-master:7077

      - JUPYTER_TOKEN=${JUPYTER_TOKEN:-lesson-spark-2026}

      - PYSPARK_SUBMIT_ARGS=--master spark://spark-master:7077 --packages org.apache.hadoop:hadoop-aws:3.3.4,com.amazonaws:aws-java-sdk-bundle:1.12.262 pyspark-shell

    command: >
      bash -lc "
        if [ ! -x /opt/conda/envs/pyspark38/bin/python ]; then
          conda create -y -n pyspark38 python=3.8 ipykernel;
        fi &&
        /opt/conda/envs/pyspark38/bin/python -m ipykernel install --user --name pyspark38 --display-name 'Python 3.8 (Spark worker-ə uyğun)' &&
        start-notebook.sh
      "
    networks:
      - lake
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl --silent --fail http://localhost:8888/api || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

networks:
  lake:

volumes:
  minio_data:
  spark_events: